const fs = require('fs-extra');
const path = require('path');

const srcDir = path.join(__dirname, 'template/setup/');

fs.copy(srcDir, './', function (err) {
    if (err) {
      console.error(err);
    } else {
      console.log("success!");
    }
});